# Dependencies
node_modules/

# TypeScript compiled output (intermediate — not the bundled dist/)
lib/

# NOTE: dist/ is NOT ignored — GitHub Actions requires it to be committed.
# The action.yml entry point is dist/index.js, and Actions does not run npm install.

# Coverage
coverage/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Secrets (NEVER commit)
.secrets
.env
*.pem
*.key
*.pfx
